import React from "react";
import "./styles.css"

const Queque = ({ type, queque, name, handlePlaylist }) => {
    return (
        <React.Fragment>
            {/* <QuequePreview type={type} queque={queque} />
            <QuequeContent name={name} handlePlaylist={handlePlaylist} /> */}
        </React.Fragment>
    );
}

export default Queque;





